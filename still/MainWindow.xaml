<Window x:Class="Still.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework" Title="Still" Width="1260" Height="810" MinWidth="760" MinHeight="500" WindowStartupLocation="CenterScreen" WindowStyle="None" ResizeMode="CanResize" UseLayoutRounding="True" SnapsToDevicePixels="True" PreviewKeyDown="WindowKeyDown">
 <shell:WindowChrome.WindowChrome><shell:WindowChrome CaptionHeight="0" ResizeBorderThickness="6" GlassFrameThickness="0" CornerRadius="12" UseAeroCaptionButtons="False"/></shell:WindowChrome.WindowChrome>
 <Border x:Name="Frame" BorderThickness="1" BorderBrush="{DynamicResource Line}" Background="{DynamicResource Ground}" TextOptions.TextFormattingMode="Display" TextOptions.TextRenderingMode="Grayscale">
  <Grid>
   <Grid.RowDefinitions><RowDefinition Height="52"/><RowDefinition x:Name="StripRow" Height="0"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
   <Grid x:Name="Toolbar" Background="{DynamicResource Ground}" MouseLeftButtonDown="DragWindow">
    <Grid.ColumnDefinitions><ColumnDefinition Width="110"/><ColumnDefinition Width="104"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
     <Button Style="{StaticResource Traffic}" Background="#FF6259" Content="×" ToolTip="Close Still" AutomationProperties.Name="Close Still" Click="CloseWindow"/>
     <Button Style="{StaticResource Traffic}" Background="#FFBE2F" Content="−" ToolTip="Minimize" AutomationProperties.Name="Minimize" Click="MinimizeWindow"/>
     <Button Style="{StaticResource Traffic}" Background="#2ACB42" Content="+" ToolTip="Maximize or restore" AutomationProperties.Name="Maximize or restore" Click="MaximizeWindow"/>
    </StackPanel>
    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
     <Button x:Name="BackButton" Style="{StaticResource IconButton}" Content="&#xE72B;" ToolTip="Back · Alt + Left" AutomationProperties.Name="Back" Click="BackClick"/>
     <Button x:Name="ForwardButton" Style="{StaticResource IconButton}" Content="&#xE72A;" ToolTip="Forward · Alt + Right" AutomationProperties.Name="Forward" Click="ForwardClick"/>
     <Button x:Name="ReloadButton" Style="{StaticResource IconButton}" Content="&#xE72C;" ToolTip="Reload · Ctrl + R" AutomationProperties.Name="Reload" Click="ReloadClick"/>
    </StackPanel>
    <Button x:Name="AddressButton" Grid.Column="2" Margin="32,8,24,8" MaxWidth="580" Background="{DynamicResource Sidebar}" HorizontalAlignment="Stretch" Click="AddressClick" ToolTip="Search or enter an address · Ctrl + L" AutomationProperties.Name="Address bar">
     <StackPanel Orientation="Horizontal"><TextBlock x:Name="SecurityGlyph" FontFamily="Segoe MDL2 Assets" FontSize="11" Text="&#xE721;" Foreground="{DynamicResource Muted}" VerticalAlignment="Center" Margin="0,0,9,0"/><TextBlock x:Name="AddressLabel" Text="Search or enter an address" Foreground="{DynamicResource Muted}" TextTrimming="CharacterEllipsis" MaxWidth="390"/></StackPanel>
    </Button>
    <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,14,0">
     <Button x:Name="BookmarkButton" Style="{StaticResource IconButton}" Content="&#xE734;" ToolTip="Bookmark this page · Ctrl + D" AutomationProperties.Name="Bookmark this page" Click="BookmarkClick"/>
     <Button x:Name="ShieldButton" Style="{StaticResource IconButton}" Content="&#xE72E;" ToolTip="Site controls" AutomationProperties.Name="Site controls" Click="ShieldClick"/>
     <Button Style="{StaticResource IconButton}" Content="&#xE8A5;" ToolTip="Reading mode · Ctrl + Shift + R" AutomationProperties.Name="Reading mode" Click="ReaderClick"/>
     <Button Style="{StaticResource IconButton}" Content="&#xE712;" ToolTip="Menu" AutomationProperties.Name="Menu" Click="MenuClick"/>
    </StackPanel>
   </Grid>
   <Border Grid.Row="1" Background="{DynamicResource Ground}" BorderBrush="{DynamicResource Line}" BorderThickness="0,0,0,1"><ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled"><StackPanel x:Name="TopTabs" Orientation="Horizontal" Margin="12,3"/></ScrollViewer></Border>
   <Grid Grid.Row="2">
    <Grid.ColumnDefinitions><ColumnDefinition x:Name="SideColumn" Width="220"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
    <Border x:Name="SidebarPanel" Background="{DynamicResource Sidebar}" BorderBrush="{DynamicResource Line}" BorderThickness="0,0,1,0">
     <DockPanel Margin="12,6,12,10">
      <StackPanel DockPanel.Dock="Bottom">
       <Border Height="1" Background="{DynamicResource Line}" Margin="4,14,4,10"/>
       <Grid><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
        <StackPanel Margin="9,4"><TextBlock Text="still" FontSize="17" FontWeight="SemiBold"/><TextBlock Text="A quiet browser" FontSize="11" Margin="0,2,0,0" Foreground="{DynamicResource Muted}"/></StackPanel>
        <Button Grid.Column="1" Style="{StaticResource IconButton}" Content="&#xE81C;" ToolTip="Switch light / dark" AutomationProperties.Name="Switch theme" Click="ThemeClick"/>
        <Button Grid.Column="2" Style="{StaticResource IconButton}" Content="&#xE713;" ToolTip="Settings · Ctrl + ," AutomationProperties.Name="Settings" Click="SettingsClick"/>
       </Grid>
      </StackPanel>
      <StackPanel DockPanel.Dock="Top"><WrapPanel x:Name="PinPanel" Margin="0,6,0,10"/><TextBlock x:Name="TabsCaption" Text="YOUR TABS" FontSize="10" FontWeight="SemiBold" Foreground="{DynamicResource Muted}" Margin="10,6,0,10"/></StackPanel>
      <ScrollViewer><StackPanel><StackPanel x:Name="TabPanel"/><Button Margin="0,5,0,0" Padding="10,10" HorizontalContentAlignment="Left" Click="NewTabClick" AutomationProperties.Name="New tab"><StackPanel Orientation="Horizontal"><TextBlock Text="+" FontSize="18" Foreground="{DynamicResource Muted}" Margin="1,-3,12,0"/><TextBlock Text="New tab" Foreground="{DynamicResource Muted}"/><TextBlock Text="Ctrl T" FontSize="11" Foreground="{DynamicResource Muted}" Margin="43,1,0,0"/></StackPanel></Button></StackPanel></ScrollViewer>
     </DockPanel>
    </Border>
    <Grid Grid.Column="1" x:Name="ContentArea" Background="{DynamicResource Surface}">
     <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
     <Border x:Name="FindBar" Visibility="Collapsed" Background="{DynamicResource Ground}" Padding="12,6"><DockPanel><Button DockPanel.Dock="Right" Content="×" Click="CloseFind"/><Button DockPanel.Dock="Right" Content="Next" Click="FindNext"/><TextBox x:Name="FindInput" Width="260" HorizontalAlignment="Right" KeyDown="FindKeyDown" AutomationProperties.Name="Find in page"/></DockPanel></Border>
     <Grid Grid.Row="1" x:Name="PageArea">
      <Grid x:Name="WebHost"/>
      <Grid x:Name="StartPage" Background="{DynamicResource Surface}">
       <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="30,0,30,70" MaxWidth="470">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,26"><Border Width="9" Height="36" CornerRadius="4.5" Background="{DynamicResource Ink}"/><Border Width="9" Height="36" CornerRadius="4.5" Background="{DynamicResource Ink}" Margin="7,0,0,0"/></StackPanel>
        <TextBlock Text="still." FontFamily="Segoe UI Variable Display, Segoe UI" FontSize="58" FontWeight="Light" HorizontalAlignment="Center"/>
        <TextBlock x:Name="Greeting" Text="Where to?" Foreground="{DynamicResource Muted}" FontSize="15" HorizontalAlignment="Center" Margin="0,8,0,30"/>
        <Button Width="430" Height="48" Background="{DynamicResource Ground}" BorderThickness="1" BorderBrush="{DynamicResource Line}" Click="AddressClick" AutomationProperties.Name="Search from new tab"><Grid Width="392"><TextBlock Text="Search or enter an address" Foreground="{DynamicResource Muted}" VerticalAlignment="Center"/><Border BorderThickness="1" BorderBrush="{DynamicResource Line}" CornerRadius="4" Padding="5,2" HorizontalAlignment="Right"><TextBlock Text="Ctrl L" FontSize="11" Foreground="{DynamicResource Muted}"/></Border></Grid></Button>
        <TextBlock Text="A little less browser. A little more internet." FontSize="12" Foreground="{DynamicResource Muted}" HorizontalAlignment="Center" Margin="0,24,0,0"/>
       </StackPanel>
       <TextBlock x:Name="PrivateNote" Visibility="Collapsed" Text="Private tab · History and this tab won't be saved." Foreground="{DynamicResource Muted}" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,35"/>
      </Grid>
      <Border x:Name="SheetBackdrop" Visibility="Collapsed" Background="{DynamicResource Ground}" MouseLeftButtonDown="BackdropClick">
       <Border x:Name="Sheet" Background="{DynamicResource Surface}" BorderBrush="{DynamicResource Line}" BorderThickness="1" CornerRadius="12" Padding="28" Width="550" MaxHeight="620" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="24,36,24,24" MouseLeftButtonDown="SheetClick">
        <DockPanel LastChildFill="True"><Grid DockPanel.Dock="Top" Margin="0,0,0,22"><TextBlock x:Name="SheetTitle" Text="Settings" FontSize="23" FontWeight="SemiBold"/><Button HorizontalAlignment="Right" Content="×" Width="30" Height="30" Padding="0" FontSize="20" Click="CloseSheetClick" ToolTip="Close · Esc" AutomationProperties.Name="Close panel"/></Grid><ScrollViewer><StackPanel x:Name="SheetContent"/></ScrollViewer></DockPanel>
       </Border>
      </Border>
      <ProgressBar x:Name="LoadProgress" Visibility="Collapsed" VerticalAlignment="Top" Height="2" IsIndeterminate="True" Foreground="#919D8B" Background="Transparent" BorderThickness="0"/>
     </Grid>
    </Grid>
   </Grid>
   <Border Grid.Row="3" x:Name="ToastBar" Visibility="Collapsed" Background="{DynamicResource Sidebar}" Padding="16,7"><TextBlock x:Name="ToastText" Foreground="{DynamicResource Muted}" FontSize="12" TextWrapping="Wrap"/></Border>
  </Grid>
 </Border>
</Window>
